enum AppBarAction { qrCodeScanner, notification }

enum Filters { distance, theme, location, facilities, other }

enum Status { pending, redeemed, expired, transferred, none }

enum ChatTypes { all, primary, general, request, group }
